---
groups:
  app:
  - mcc-front
  - mcc-api
  cli:
  - snk-cli
  - mcc-cli
  stores_kv:
  - mcc-redis

commands:
  test: run mcc-cli make test
  build: run mcc-cli make build
  gofmt: run mcc-cli gofmt -w crane

accelerated-mounts:
  crane:
    uid: 1000
    gid: 1000